#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../handlebars/bin/handlebars" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../handlebars/bin/handlebars" "$@"
  ret=$?
fi
exit $ret
